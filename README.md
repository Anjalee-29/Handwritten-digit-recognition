
# 🔢 MNIST Digit Recognizer

A neural network built with Keras and TensorFlow that recognizes handwritten digits from the MNIST dataset. Draw a digit on screen and the model predicts what it is in real time!

---

## 🛠️ Description

This project has two parts:

- **`main.py`** — Trains a fully connected neural network on the MNIST dataset (60,000 handwritten digit images) and saves the model
- **`predict.py`** — Opens a drawing canvas where you can draw a digit and the model predicts it with a confidence score

The model architecture:
- Dense layer with 512 neurons and ReLU activation
- Dropout layer (20%) to prevent overfitting
- Softmax output layer for 10-class classification
- Early stopping to halt training when validation loss stops improving

---

## ⚙️ Requirements

- Python 3.8+
- TensorFlow 2.20+
- Keras 3.0+
- Pillow
- tkinter (built into Python on Windows/macOS)

Install all dependencies:

```bash
pip install -r requirements.txt
```

> **Linux users** may need to install tkinter separately:
> ```bash
> sudo apt-get install python3-tk
> ```

---

## 🚀 How to Run

Clone the repository:

```bash
git clone https://github.com/Anjalee-29/Handwritten-digit-recognition.git
cd Handwritten-digit-recognition
pip install -r requirements.txt
```

### Step 1 — Train the model:

```bash
python main.py
```

This downloads the MNIST dataset automatically, trains the model, and saves it as `mnist_model.keras`.

### Step 2 — Recognize a digit:

```bash
python predict.py
```

- Draw a digit on the black canvas using your mouse
- Click **Predict** to see the result and confidence score
- Click **Clear** to draw again

---

## 📊 Expected Results

| Metric | Value |
|---|---|
| Test Accuracy | ~98% |
| Test Loss | ~0.07 |

---

## 📁 Project Structure

```
├── main.py              # Model training and evaluation
├── predict.py           # Drawing GUI for digit recognition
├── requirements.txt     # Dependencies
├── .gitignore           # Excludes cache and model files
└── README.md
```

---

## 📝 Notes

- You must run `main.py` first to generate `mnist_model.keras` before using `predict.py`
- The trained model file is excluded from the repo via `.gitignore` — it can be regenerated by running `main.py`
- Draw digits large and centered on the canvas for best accuracy

---
